## Vue APP for Plato Creative

This is a single page application which fetches movies from TMDB API and lists them in a grid. The application also allows to search for a particular movie.

## To Install
* clone the repo
* run `npm install`
* run `npm run dev`